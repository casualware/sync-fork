{
  "name": "@casualware/sync-fork",
  "version": "1.0.0",
  "description": "A GitHub action that can sync a fork with its upstream repository",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && esbuild src/main.ts --bundle --platform=node --outfile=dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/casualware/sync-fork.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/casualware/sync-fork/issues"
  },
  "homepage": "https://github.com/casualware/sync-fork#readme",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "20.12.7",
    "typescript": "^5.4.5",
    "esbuild": "^0.20.2"
  }
}
